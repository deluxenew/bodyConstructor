export const constants = () => {
  const sideDepth = .16;
  const legsHeight = 1;
  const legsRad = 0.3;
  const legFrontMargin = 0.38;
  const sideTop = 0.8;
  const scale = 1

  const drawerTopGap = 0.48; // верхний отступ выдвижного ящика
  const drawerTopGapBig = 0.64; // верхний бОльший отступ выдвижного ящика
  const drawerSideGap = 0.15;  //боковой отступ выдвижного ящика
  const drawerBottomGap = 0.12;  //нижний отступ выдвижного ящика
  const drawerSideDepth = 0.02; // толщина стенки выдвижного ящика
  const drawerBetweenGap = 0.94; 

  const bottomHeight = 8.2
  const bottomDepth = 4.8

  const topHeight = 7.2
  const topDepth = 3

  const topBound = 21.40

  return {
    sideDepth,
    legsHeight,
    legsRad,
    legFrontMargin,
    sideTop,
    scale,
    bottomHeight,
    bottomDepth,
    drawerSideGap,
    drawerBottomGap,
    drawerSideDepth,
    drawerTopGap,
    drawerTopGapBig,
    drawerBetweenGap,
    topHeight,
    topDepth,
    topBound

  }
}

